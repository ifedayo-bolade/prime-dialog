<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LibDialog" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Theme.LibDialog" parent="Base.Theme.LibDialog" />

    <style name="MyDialogAnimationStyle" parent="Theme.Material3.DayNight.Dialog.Alert">
        <item name="android:windowEnterAnimation">@anim/rotate_fade_in</item>
        <item name="android:windowExitAnimation">@anim/rotate_fade_out</item>
    </style>

</resources>